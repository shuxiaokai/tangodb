<%= "#{@videos_filtered.count} Results" %>
<div data-controller="toggle">
  <button type='submit' data-action="toggle#toggle" class="filter_button">
    <i class="fas fa-filter fa-lg" aria-hidden="true"></i>
  </button>

  <div class="hidden" data-toggle-target="toggleable">

    <div class="filter_container" data-controller="filter">
      <div data-controller="choices" data-item-select-text=" " data-should-sort="false" data-should-sort-items="false" data-position="down">
        <div class="filter_item">
          <label>Type</label>
          <datalist data-choices-target="options">
            <%= options_for_select([["Select All #{@videotypes.count} Types", " "]] + @videotypes,
              params[:videotype].presence || " ") %>
          </datalist>
          <%= select_tag :videotype, 
                options_for_select(@videotypes), 
                multiple: false, 
                size: 12, 
                data: { "choices-target": "select", action: "filter#filter", "filter-target": "filter" } %>
        </div>
      </div>
      <div data-controller="choices" data-item-select-text=" " data-should-sort="false" data-should-sort-items="false" data-position="down">
        <div class="filter_item">
          <label>Genre</label>
          <datalist data-choices-target="options">
            <%= options_for_select([["Select All #{@genres.count} Genres", " "]] + @genres,
              params[:genre].presence || " ") %>
          </datalist>
          <%= select_tag :genre, 
                options_for_select(@genres), 
                multiple: false, 
                size: 12, 
                data: { "choices-target": "select", action: "filter#filter", "filter-target": "filter" } %>
        </div>
      </div>
      <div data-controller="choices" data-item-select-text=" " data-should-sort="false" data-should-sort-items="false" data-position="down">
        <div class="filter_item">
          <label>Leader</label>
          <datalist data-choices-target="options">
            <%= options_for_select([["Select All #{@leaders.count} leaders", " "]] + @leaders,
              params[:leader].presence || " ") %>
          </datalist>
          <%= select_tag :leader, 
                options_for_select(@leaders), 
                multiple: false, 
                size: 12, 
                data: { "choices-target": "select", action: "filter#filter", "filter-target": "filter" } %>
        </div>
      </div>
      <div data-controller="choices" data-item-select-text=" " data-should-sort="false" data-should-sort-items="false" data-position="down">
        <div class="filter_item">
          <label>Follower</label>
          <datalist data-choices-target="options">
            <%= options_for_select([["Select All #{@followers.count} followers", " "]] + @followers,
              params[:follower].presence || " ") %>
          </datalist>
          <%= select_tag :follower, 
                options_for_select(@followers), 
                multiple: false, 
                size: 12, 
                data: { "choices-target": "select", action: "filter#filter", "filter-target": "filter" } %>
        </div>
      </div>
      <div data-controller="choices" data-item-select-text=" " data-should-sort="false" data-should-sort-items="false" data-position="down">
        <div class="filter_item">
          <label>Vvent</label>
          <datalist data-choices-target="options">
            <%= options_for_select([["Select All #{@events.count} events", " "]] + @events,
              params[:event].presence || " ") %>
          </datalist>
          <%= select_tag :event, 
                options_for_select(@events), 
                multiple: false, 
                size: 12, 
                data: { "choices-target": "select", action: "filter#filter", "filter-target": "filter" } %>
        </div>
      </div>
      <div data-controller="choices" data-item-select-text=" " data-should-sort="false" data-should-sort-items="false" data-position="down">
        <div class="filter_item">
          <label>Channel</label>
          <datalist data-choices-target="options">
            <%= options_for_select([["Select All #{@channels.count} channels", " "]] + @channels,
              params[:channel].presence || " ") %>
          </datalist>
          <%= select_tag :channel, 
                options_for_select(@channels), 
                multiple: false, 
                size: 12, 
                data: { "choices-target": "select", action: "filter#filter", "filter-target": "filter" } %>
        </div>
      </div>
    </div>
  </div>
</div>
</div>