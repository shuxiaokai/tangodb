<p id="notice"><%= notice %></p>

<p>
  <%= @post.title %>
  <%= @post.content %>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
dates = []
Video.all.each do |video|
  performance_date = Date.parse(video.description)
  rescue  e
  next if performance_date.nil?
  dates << performance_date
end